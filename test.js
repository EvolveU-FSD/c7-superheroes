// const { createTable, create, findAll } = require('./server/models/superheroSQL')
// // // createTable()
// // // create({ name: 'bobby', nickname: '233', alterego: 'bibi', sidekick: 'blibi' })
// let m = findAll()
// m.then(rows=>console.log(rows))

